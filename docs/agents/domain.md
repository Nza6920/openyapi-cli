# Domain docs

This is a single-context repository.

Before exploring a topic, read the root `CONTEXT.md` when present and any relevant records under `docs/adr/`. If they do not exist, proceed silently. Use the glossary vocabulary defined by `CONTEXT.md`, and flag conflicts with existing ADRs instead of overriding them silently.
